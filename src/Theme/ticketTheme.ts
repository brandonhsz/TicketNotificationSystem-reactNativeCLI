import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  ticketContainer: {
    borderWidth: 1,
    borderColor: "thistle",
    borderRadius: 10,
    padding: 10,
    margin: 2,
    minWidth: "90%",
    maxWidth: "90%"
  },
  dataText: {
    color: "white",
    fontFamily: "monospace",
  },
});